---
import NewsletterSignupIsland from './react/NewsletterSignup';
import { site } from '../lib/site';

interface Props {
  variant?: string;
}

const { variant = 'default' } = Astro.props;
---

<NewsletterSignupIsland client:idle endpoint={site.newsletterEndpoint} variant={variant} />
